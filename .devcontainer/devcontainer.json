{
  "name": "Gafu Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:22",
  "features": {},
  "postCreateCommand": "curl -fsSL https://bun.sh/install | bash && sudo ln -sf /home/node/.bun/bin/bun /usr/local/bin/bun && rm -f node_modules && bun install",
  "runArgs": ["--net=host"],
  "customizations": {
    "vscode": {
      "extensions": [
        "oven.bun-vscode"
      ]
    }
  },
  "remoteUser": "node"
}
